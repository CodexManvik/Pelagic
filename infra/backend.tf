# Local state is the default for Terraform and is used now.
# To move to remote state later, replace this block with:
# terraform {
#   backend "gcs" {
#     bucket = "your-terraform-state-bucket"
#     prefix = "project-leviathan/infra"
#   }
# }
